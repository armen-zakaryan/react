'use strict';

define(['react', 'jsx!react-components/map'], function(React, Map) {
    /*
    React.render 	( < TimerExample start = {Date.now()}/>,
        document.getElementById('example')
    );
*/

    
    React.render 	( <Map/>,
         document.getElementById('map')
    );
    
    
 

});
